import React from 'react';
import { AlertTriangle, X, ShieldAlert, CheckCircle2 } from 'lucide-react';

export default function DisclaimerModal({ isOpen, onClose }) {
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/70 backdrop-blur-sm animate-fade-in">
      <div className="bg-slate-900 border border-slate-700 rounded-2xl max-w-lg w-full p-6 shadow-2xl relative text-slate-200">
        <button
          onClick={onClose}
          className="absolute top-4 right-4 text-slate-400 hover:text-white p-1 rounded-lg hover:bg-slate-800 transition"
        >
          <X className="w-5 h-5" />
        </button>

        <div className="flex items-center gap-3 mb-4 text-amber-400">
          <div className="p-2.5 bg-amber-500/10 border border-amber-500/20 rounded-xl">
            <ShieldAlert className="w-6 h-6" />
          </div>
          <div>
            <h3 className="text-lg font-bold text-white">Financial Disclaimer & Caveat</h3>
            <p className="text-xs text-amber-400/80 font-medium">Educational & Portfolio Demonstration Only</p>
          </div>
        </div>

        <div className="space-y-3 text-sm text-slate-300 leading-relaxed border-y border-slate-800 py-4 my-2">
          <p>
            <strong className="text-white">1. Not Financial Advice:</strong> The predictions generated by this application are produced by Long Short-Term Memory (LSTM) recurrent neural networks trained on historical price sequences. They are for educational and machine learning exploration purposes only.
          </p>
          <p>
            <strong className="text-white">2. Random Walk & Market Uncertainty:</strong> Financial markets are influenced by macroeconomic shifts, unexpected news, earnings releases, and non-stationary dynamics. Price forecasting models carry significant uncertainty and must never be used to make actual trading or investment decisions.
          </p>
          <p>
            <strong className="text-white">3. Baseline Comparison:</strong> To ensure complete transparency, this application always compares the LSTM model's test performance against a naive persistence baseline (<em className="text-cyan-400">predicting tomorrow = today</em>).
          </p>
        </div>

        <div className="mt-4 flex justify-end">
          <button
            onClick={onClose}
            className="px-5 py-2.5 bg-cyan-600 hover:bg-cyan-500 text-white font-medium text-sm rounded-xl transition shadow-lg shadow-cyan-600/20 cursor-pointer"
          >
            I Understand
          </button>
        </div>
      </div>
    </div>
  );
}
